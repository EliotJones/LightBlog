@model PagedPostsViewModel
@{
    ViewData["Title"] = "Home Page";
}

@Html.Partial("_Paging")

@for(var i = 0; i < Model.Posts.Count; i++)
{
    @Html.Partial("_Post", Model.Posts[i], new ViewDataDictionary(ViewData) { { "home", true } })
}

@Html.Partial("_Paging")